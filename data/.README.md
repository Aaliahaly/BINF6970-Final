# TCGA LGG Multi-Omics Database Dataset Links and Pipeline Documentation

The datasets used in this project are hosted externally on Figshare because several files are too large to store directly in GitHub.

Each Figshare dataset corresponds to a specific stage of the project workflow, from selected input files to cleaned data, validated data, harmonized data, final integrated datasets, SQL database population, and graph database construction.

---

## Figshare Dataset Links

| Dataset Stage | Figshare Dataset Title | Link |
|---|---|---|
| Python Pipeline Input Files | `TCGA_LGG_Python_Pipeline_Input_Files` | https://doi.org/10.6084/m9.figshare.32194455 |
| Cleaned Data | `01_Cleaned_Data` | https://doi.org/10.6084/m9.figshare.32194497 |
| Validated Data | `02_Validated_Data` | https://doi.org/10.6084/m9.figshare.32194545 |
| Sample-Harmonized Data | `03_Sample_Harmonized_Data` | https://doi.org/10.6084/m9.figshare.32194617 |
| Gene-Harmonized Data | `04_Gene_Harmonized_Data` | https://doi.org/10.6084/m9.figshare.32194650 |
| HGNC-Mapped Data | `05_HGNC_Mapped_Data` | https://doi.org/10.6084/m9.figshare.32194674 |
| Final Harmonized Data | `06_Final_Harmonized_Data` | https://doi.org/10.6084/m9.figshare.32194689 |
| Final Proof Report | `07_Validation_Report` | https://doi.org/10.6084/m9.figshare.32194704 |
| SQL Database Files | `SQL_database_files` | https://doi.org/10.6084/m9.figshare.32204706 |
| Graph Database Files | `Neo4j_Graph_Database_Files` | https://doi.org/10.6084/m9.figshare.32206935 |

---

# Pipeline Overview

The project converts selected TCGA Lower Grade Glioma source data into cleaned, validated, harmonized, database-ready, and graph-ready datasets.

The full project workflow includes the following stages:

1. Data cleaning and transformation
2. Dataset validation
3. Sample-level harmonization
4. Gene-level harmonization
5. HGNC-based gene identifier mapping
6. Final dataset harmonization
7. Final proof report generation
8. Database population
9. Graph database population

Stages 1 to 7 are executed by `run_pipeline.py`.

Stages 8 and 9 are post-pipeline database implementation stages and are executed separately.

---

# Python Pipeline Input Files

The input dataset contains the selected files used as starting material for the Python processing pipeline.

These files were prepared from selected source files from the original TCGA LGG dataset downloaded from cBioPortal. They provide the clinical, sample-level, mutation, copy number alteration, expression, and hypoxia-related information required for downstream processing.

The source files used to prepare the pipeline input files include:

- `data_clinical_patient.txt`
- `data_clinical_sample.txt`
- `data_clinical_supp_hypoxia.txt`
- `data_cna.txt.zip`
- `data_mutations.txt.zip`

These files are not the complete original TCGA LGG download. They represent the selected project-specific input files needed for the pipeline.

Figshare link: https://doi.org/10.6084/m9.figshare.32194455

---

# 1. Data Cleaning and Transformation

This stage cleans and transforms the selected input files into structured datasets suitable for downstream validation, harmonization, and database construction.

---

## 1.1 Clinical and Sample Data Cleaning

| Item | Details |
|---|---|
| Input dataset | `Clinical&Sample(1).xlsx` |
| Output dataset | `Clinical&Sample.xlsx` |
| Script | `01_clean_clinical_sample.py` |
| Stage | Cleaning |

This script cleans and standardizes clinical and sample data into a structured format ready for analysis and database integration.

It renames variables into consistent database-aligned names, converts encoded survival variables into clear clinical labels, rounds time-related fields, standardizes cancer type and site naming, and extracts IDH status and 1p/19q codeletion status from subtype information.

It also filters and orders the dataset into a fixed schema to improve readability, visualization, and downstream database use.

Main changes include:

- Standardized all column names into database-friendly format.
- Renamed survival variables into clear clinical terms.
- Converted binary survival encodings into explicit clinical labels.
- Rounded survival time variables to two decimal places.
- Standardized cancer type and cancer site fields.
- Renamed histological classification fields.
- Split subtype information into `IDH_Status` and `Codeletion_Status`.
- Standardized hypoxia-related variables.
- Removed whitespace inconsistencies.
- Verified uniqueness using `Patient_ID` and `Sample_ID`.

---

## 1.2 Copy Number Alteration Data Cleaning

| Item | Details |
|---|---|
| Input dataset | `data_cna.txt` |
| Output dataset | `cna_long.csv` |
| Script | `02_clean_cna.py` |
| Stage | Cleaning |

This script transforms copy number alteration data from wide matrix format into long format, where each row represents one gene-sample pair.

It standardizes gene identifiers, converts numeric copy number alteration values into biological CNA status labels, handles missing or invalid values, removes exact duplicate rows, and prepares the dataset for integration with mutation, expression, and clinical data.

Main changes include:

- Converted CNA data from wide format into long format.
- Preserved `Hugo_Symbol` and `Entrez_Gene_Id` as fixed gene-level identifiers.
- Created `Sample_ID` to store sample identifiers after transformation.
- Stored numeric CNA values in `CNA_Value`.
- Converted `CNA_Value` into numeric format.
- Created `CNA_Status` to store biological CNA labels.
- Mapped numeric CNA values into biological labels:

  - `-2` = `deep_loss`
  - `-1` = `loss`
  - `0` = `neutral`
  - `1` = `gain`
  - `2` = `amplification`

- Assigned missing, invalid, or unexpected CNA values as `unknown`.
- Reordered columns into a clean structure:

  - `Hugo_Symbol`
  - `Entrez_Gene_Id`
  - `Sample_ID`
  - `CNA_Value`
  - `CNA_Status`

- Removed exact duplicate rows.
- Verified dataset size before and after duplicate removal.

---

## 1.3 Gene Expression Data Cleaning

| Item | Details |
|---|---|
| Input dataset | `data_mrna_seq_v2_rsem.txt` |
| Output dataset | `long_expression.csv` |
| Script | `03_clean_expression.py` |
| Stage | Cleaning |

This script converts RNA-seq gene expression data from wide format into long format, where each row represents a gene-sample pair.

It standardizes gene identifiers, cleans and rounds expression values, removes zero-expression rows, removes duplicate rows, and outputs a structured dataset ready for downstream multi-omics integration.

Main changes include:

- Converted expression data from wide format into long format.
- Standardized `Hugo_Symbol`, `Entrez_Gene_Id`, and `Sample_ID`.
- Renamed the expression value column to `Expression`.
- Converted expression values into numeric format.
- Rounded expression values to two decimal places.
- Removed rows with zero expression values.
- Removed exact duplicate rows.
- Verified dataset size before and after duplicate removal.

---

## 1.4 Mutation Data Cleaning

| Item | Details |
|---|---|
| Input dataset | `Mutation(1).xlsx` |
| Output dataset | `Mutation.xlsx` |
| Script | `04_clean_mutations.py` |
| Stage | Cleaning |

This script processes mutation data by standardizing mutation annotations, selecting the most severe variant consequence for each mutation using VEP severity ranking, and calculating Variant Allele Frequency.

Main changes include:

- Renamed key fields to match the database schema.
- Renamed `Consequence` to `Primary_Consequence`.
- Renamed `IMPACT` to `Impact_Level`.
- Selected the most severe consequence using VEP severity ranking.
- Standardized mutation annotation fields.
- Converted read count fields into numeric format.
- Calculated Variant Allele Frequency using:

```text
VAF = t_alt_count / (t_ref_count + t_alt_count)
```

---

# 2. Dataset Validation

After the initial cleaning stage, all cleaned datasets were validated to confirm that they were structurally consistent and ready for harmonization.

This stage checks the presence of required identifiers such as `Sample_ID` and `Hugo_Symbol`, verifies that critical fields are not missing or empty, standardizes gene symbol formatting, and confirms that each file contains the required columns for downstream processing.

The HGNC reference file was also validated to ensure that it contains the fields needed for gene identifier mapping.

This step does not change the biological meaning of the data. It focuses on data integrity, identifier quality, and structural readiness.

| Item | Details |
|---|---|
| Input datasets | `Clinical&Sample.xlsx`, `long_expression.csv`, `Mutation.xlsx`, `cna_long.csv`, `hgnc_complete_set.txt` |
| Output datasets | Validated clinical, expression, mutation, CNA, and HGNC reference files |
| Script | `05_validator.py` |
| Stage | Validation |

Figshare link: https://doi.org/10.6084/m9.figshare.32194545

---

# 3. Sample-Level Harmonization

This stage harmonizes the validated datasets at the sample level.

The script cleans and standardizes `Sample_ID` values across the clinical, expression, CNA, and mutation datasets. It then identifies the common set of samples shared across these datasets and filters each dataset to retain only matched samples.

This ensures that every retained sample has consistent representation across the required multi-omics data types. The resulting files are sample-matched and ready for gene-level harmonization.

| Item | Details |
|---|---|
| Input datasets | `Clinical&Sample.xlsx`, `long_expression.csv`, `Mutation.xlsx`, `cna_long.csv` |
| Output datasets | `clin_step1.xlsx`, `expr_step1.csv`, `cna_step1.csv`, `mut_step1.xlsx` |
| Script | `06_sample_harmonization.py` |
| Stage | Sample-level harmonization |

Figshare link: https://doi.org/10.6084/m9.figshare.32194617

---

# 4. Gene-Level Harmonization

This stage harmonizes the molecular datasets at the gene level.

The script standardizes `Hugo_Symbol` values across the expression, CNA, and mutation datasets. It then identifies the common set of genes shared across these molecular data types and filters each dataset to retain only those genes.

This step ensures that expression, CNA, and mutation records are based on a matched gene set, which supports reliable multi-omics integration and analysis.

| Item | Details |
|---|---|
| Input datasets | `expr_step1.csv`, `cna_step1.csv`, `mut_step1.xlsx` |
| Output datasets | `expr_step2.csv`, `cna_step2.csv`, `mut_step2.xlsx` |
| Script | `07_gene_harmonization.py` |
| Stage | Gene-level harmonization |

Figshare link: https://doi.org/10.6084/m9.figshare.32194650

---

# 5. HGNC-Based Gene Identifier Mapping

This stage standardizes gene identifiers using HGNC reference information.

The script maps `Hugo_Symbol` values to official HGNC-approved gene symbols and Entrez Gene IDs. It also uses alias symbols and previous gene names to resolve naming inconsistencies across the molecular datasets.

Unmapped or invalid gene entries are removed to ensure that all retained molecular records are linked to reliable standardized gene identifiers.

This step improves consistency across expression, CNA, and mutation datasets and prepares them for final integration.

| Item | Details |
|---|---|
| Input datasets | `expr_step2.csv`, `cna_step2.csv`, `mut_step2.xlsx`, `hgnc_complete_set.txt` |
| Output datasets | `expr_step3.csv`, `cna_step3.csv`, `mut_step3.xlsx` |
| Script | `08_hgnc_mapping.py` |
| Stage | HGNC mapping |

Figshare link: https://doi.org/10.6084/m9.figshare.32194674

---

# 6. Final Harmonization

This stage performs the final alignment across all processed datasets.

The script enforces both sample-level and gene-level consistency. It retains only samples shared across the clinical, expression, CNA, and mutation datasets. It also ensures that the molecular datasets contain only the common standardized genes shared across expression, CNA, and mutation data.

The output files from this stage represent the final harmonized multi-omics datasets. These files are ready for SQL table generation, database ingestion, graph database construction, and downstream analysis.

| Item | Details |
|---|---|
| Input datasets | `expr_step3.csv`, `cna_step3.csv`, `mut_step3.xlsx`, `clin_step1.xlsx` |
| Output datasets | `expr_FINAL.csv`, `cna_FINAL.csv`, `mut_FINAL.xlsx`, `clin_FINAL.xlsx` |
| Script | `09_finalize.py` |
| Stage | Final harmonization |

Figshare link: https://doi.org/10.6084/m9.figshare.32194689

---

# 7. Final Proof Report

This stage generates a final validation and proof report for the completed pipeline.

The report summarizes key dataset metrics, including counts of patients, samples, genes, mutations, expression rows, CNA rows, and other omics records. It also checks sample-mutation relationships and confirms that the final datasets follow the logic needed for database population.

The report serves as a final data integrity check before SQL database construction and graph database modeling.

| Item | Details |
|---|---|
| Input datasets | `expr_FINAL.csv`, `cna_FINAL.csv`, `mut_FINAL.xlsx`, `clin_FINAL.xlsx` |
| Output file | `final_proof_report.txt` |
| Script | `10_report.py` |
| Stage | Final validation report |

Figshare link: https://doi.org/10.6084/m9.figshare.32194704

---

# 8. Database Population

This stage generates the SQL population script used to populate the relational MySQL database from the final harmonized datasets.

This stage is not executed by `run_pipeline.py`. It is a post-pipeline database implementation step.

The script transforms the final clinical, mutation, CNA, and expression datasets into structured SQL `INSERT` statements. It standardizes values, maps relationships between patients, samples, genes, and mutations, and removes duplicate records to match the database constraints.

The output is a normalized, ready-to-run SQL script for MySQL database population.

| Item | Details |
|---|---|
| Input datasets | `clin_FINAL.xlsx`, `mut_FINAL.xlsx`, `cna_FINAL.csv`, `expr_FINAL.csv` |
| Output file | `FINAL_POPULATE.sql` |
| Script | `SQL_generation_for_populating_the_data.py` |
| Stage | Database Population |

The generated SQL population file is large and is hosted externally on Figshare as part of the SQL database files.

In the SQL directory README, this generated file is documented as:

| SQL File | Description |
|---|---|
| `02_load_cleaned_data.sql.zip` | Compressed SQL file used to load the cleaned and harmonized data into the MySQL database. |

Figshare link for SQL database files:

```text
https://doi.org/10.6084/m9.figshare.32204706
```

This stage connects the Python data processing pipeline to the relational database implementation. It ensures that the final harmonized datasets can be inserted into MySQL in a reproducible and schema-compatible format.

To generate the SQL population file separately, run:

```text
python SQL_generation_for_populating_the_data.py
```

---

# 9. Graph Database Population

This stage generates Neo4j Cypher queries for graph database population.

This stage is not executed by `run_pipeline.py`. It is a post-pipeline graph database implementation step.

The script uses `For Neo4j.csv`, which is exported from SQL-based exploratory queries. It standardizes sample and gene data, assigns IDH status and 1p/19q codeletion attributes to sample nodes, creates gene nodes, and defines relationships between samples and genes.

The output is a clean, ready-to-run Cypher script for Neo4j graph database import and network-based analysis.

| Item | Details |
|---|---|
| Input dataset | `For Neo4j.csv` |
| Output file | `neo4j_import.cypher` |
| Script | `Neo4j.py` |
| Stage | Graph Database Population |

The graph database files are hosted externally on Figshare.

| Graph Database File | Description |
|---|---|
| `For Neo4j.csv` | CSV file exported from SQL-based exploratory queries and used as input for Neo4j graph construction. |
| `neo4j_import.cypher` | Ready-to-run Cypher script used to populate the Neo4j graph database. |

Figshare link for graph database files:

```text
https://doi.org/10.6084/m9.figshare.32206935
```

The generated Cypher file supports graph-based representation of the TCGA LGG multi-omics database.

It creates a graph structure suitable for exploring relationships between samples, genes, molecular features, IDH status, and 1p/19q codeletion status.

This stage extends the project beyond relational querying and enables network-based analysis using Neo4j.

To generate the Neo4j Cypher import file separately, run:

```text
python Neo4j.py
```

---

# 10. Database Implementation Summary

The full project produces database-ready outputs for both relational and graph database environments.

The relational database implementation uses MySQL and is supported by the SQL files documented in the SQL directory.

The graph database implementation uses Neo4j and is supported by the generated Cypher import script.

Together, these outputs allow the final harmonized TCGA LGG multi-omics data to be queried in two complementary ways:

- SQL-based relational querying for structured database analysis.
- Neo4j-based graph querying for relationship-centered and network-based analysis.

The database implementation files are documented separately to keep the GitHub repository lightweight and reproducible.

The SQL database files are hosted externally on Figshare:

```text
https://doi.org/10.6084/m9.figshare.32204706
```

The graph database files are hosted externally on Figshare:

```text
https://doi.org/10.6084/m9.figshare.32206935
```

---

# Script Availability and Execution

All Python processing and post-processing scripts are available in the `scripts` directory.

The main pipeline can be executed using:

```text
python run_pipeline.py
```

This command runs the core data processing pipeline, including:

1. Data cleaning and transformation
2. Dataset validation
3. Sample-level harmonization
4. Gene-level harmonization
5. HGNC-based gene identifier mapping
6. Final dataset harmonization
7. Final proof report generation

The following scripts are post-pipeline database implementation scripts and are not included in `run_pipeline.py`:

| Script | Purpose |
|---|---|
| `SQL_generation_for_populating_the_data.py` | Generates the SQL population script from the final harmonized datasets. |
| `Neo4j.py` | Generates the Neo4j Cypher import script from the graph-ready CSV file. |

To generate the SQL population script separately, run:

```text
python SQL_generation_for_populating_the_data.py
```

To generate the Neo4j Cypher import script separately, run:

```text
python Neo4j.py
```

The SQL database files are documented in the SQL directory and hosted externally on Figshare.

The generated SQL population file is documented in the SQL directory README as:

```text
02_load_cleaned_data.sql.zip
```

Figshare link for SQL database files: 

```text
https://doi.org/10.6084/m9.figshare.32204706
```

The graph database files are documented in this README and hosted externally on Figshare.

Figshare link for graph database files:

```text
https://doi.org/10.6084/m9.figshare.32206935
```
